# نسخة Cloudflare Pages

> هذا الفرع مستقل للنشر الخارجي. لا تستخدم فرع `main` في Cloudflare لهذه النسخة؛ اختر فرع `cloudflare-pages`.

## إعدادات النشر

| الحقل في Cloudflare Pages | القيمة |
|---|---|
| اسم المشروع | `iraqpower` أو اسم متاح مشابه |
| فرع الإنتاج | `cloudflare-pages` |
| الإطار | `Vite` |
| أمر البناء | `pnpm build:cloudflare` |
| مجلد الإخراج | `dist/public` |
| إصدار Node | `22` |

## ما تم تجهيزه

تستخدم هذه النسخة صوراً عامة ضمن `client/public/assets` بدلاً من مسارات تخزين Manus، ولذلك ستعمل الصور بعد نشر الموقع على `pages.dev`. يحتوي المجلد العام أيضاً على ملف `_redirects` لدعم روابط الواجهة، وملف `_headers` لرؤوس أمان أساسية وتخزين الصور المؤقت.

## خطوات النشر

اربط مستودع GitHub في Cloudflare Pages ثم اختر فرع `cloudflare-pages`، وأدخل القيم الموجودة في الجدول. بعد نجاح البناء، ستحصل على رابط مثل `https://iraqpower.pages.dev` إذا كان الاسم متاحاً.

لا يلزم إدخال متغيرات بيئة لهذه النسخة؛ فهي واجهة ثابتة بالكامل.
